<div class="col-12 p-3">
  <%=link_to category.name, category_path(category)%>
  <%= link_to "Delete", category_path(category), class:"btn btn-link",
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
</div>